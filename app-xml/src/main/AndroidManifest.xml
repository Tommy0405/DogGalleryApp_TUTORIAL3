<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissão necessária para a aplicação aceder à internet e comunicar com a API -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Permissão para verificar o estado da rede (online/offline) -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- Activity principal - ponto de entrada da aplicação -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity que exibe os detalhes de uma imagem específica -->
        <activity android:name=".ui.DetailsActivity" />

        <!-- Activity que lista todas as imagens marcadas como favoritas -->
        <activity android:name=".ui.FavoritesActivity" />
    </application>

</manifest>